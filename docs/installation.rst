============
Installation
============

At the command line::

    $ easy_install django-registration-email

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-registration-email
    $ pip install django-registration-email
